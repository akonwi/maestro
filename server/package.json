{
  "name": "maestro-server-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "mkdir -p tmp && ard build main.ard --out tmp/maestro-server-test",
    "test": "bun test tests/",
    "check": "biome check .",
    "check:fix": "biome check . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "^1.3.0"
  }
}
